[package]
name = "shared-types"
version = "0.1.0"
edition = "2021"
description = "Shared enums, constants, and helpers for the NOUMEN protocol"

[lib]
crate-type = ["lib"]

[dependencies]
anchor-lang = "0.30.1"
